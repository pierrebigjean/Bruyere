import Sortable from 'sortablejs';

const initSortable = () => {
  // const lists = document.querySelectorAll('.sortable');
  // lists.forEach((list) => {
  //   if (list) {
  //     Sortable.create(list, {
  //       ghostClass: "ghost",
  //       animation: 150,
  //     });
  //   }
  // })
};

export { initSortable };